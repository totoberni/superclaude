# <project>/.notebook/forbidden_imports.txt — read by `nb validate` and `nb audit`.
# One regex per line. Comments (`#`) and blanks ignored.
# Default set: deprecated quantum SDKs that orchs commonly mis-import.
# Project-specific entries can be added below; these defaults are starting points.

# Qiskit deprecated providers (replaced by qiskit-ibm-runtime)
qiskit_ibm_provider
qiskit_ibmq_provider
qiskit\.providers\.ibmq

# Old qiskit-terra namespaces removed in qiskit 1.0+
qiskit\.aqua
qiskit\.algorithms\.factorizers
qiskit\.opflow

# Common scientific-stack imports that often signal a non-spec workaround
# (uncomment per project as appropriate; e.g. example-cw forbids scipy.integrate)
# from\s+scipy\.integrate
# import\s+scipy\.integrate
# \bsolve_ivp\b
# \bodeint\b
