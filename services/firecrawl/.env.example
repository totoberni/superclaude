# Copy to .env (chmod 600) and fill in. .env is gitignored — NEVER commit it.
# Value must match what the live nuq-postgres container was initialized with
# (extract via: sg docker -c "docker inspect firecrawl-nuq-postgres-1" | grep POSTGRES_PASSWORD).
POSTGRES_PASSWORD=changeme
